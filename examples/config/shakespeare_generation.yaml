defaults:
  - _self_

data:
  url: "https://raw.githubusercontent.com/karpathy/char-rnn/master/data/tinyshakespeare/input.txt"
  cache_dir: ${hydra:runtime.cwd}/data  # Use original working directory
  max_length: 10000  # Using first 1000 characters for quick testing
  seq_length: 20

model:
  hidden_size: 64
  num_layers: 2
  num_points: 10

training:
  batch_size: 128
  num_epochs: 10
  learning_rate: 0.001
  checkpoint_every: 1  # Save checkpoint every N epochs
  sample_every: 1     # Generate sample text every N epochs

generation:
  max_length: 200
  temperature: 0.8